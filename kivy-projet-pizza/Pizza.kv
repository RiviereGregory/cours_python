MainWidget:

<MainWidget>:
    recycleView: recycleView
    RecycleView:
        id: recycleView
        viewclass: 'PizzaWidget'
        RecycleBoxLayout:
            default_size: None, dp(56)
            default_size_hint: 1, None
            size_hint_y: None
            height: self.minimum_height
            orientation: 'vertical'

<PizzaWidget>:
    Label:
        text: root.nom
    Label:
        text: root.ingredients
    Label:
        text: str(root.prix) + "€"
    Label:
        text: str(root.vegetarienne)
